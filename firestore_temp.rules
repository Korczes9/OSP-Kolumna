rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // TYMCZASOWE REGUŁY - użyj tylko do pierwszego logowania!
    // Potem zamień na firestore.rules
    
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
